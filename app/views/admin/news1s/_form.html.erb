<%= form_with(model: admin_news1) do |form| %>
  <% if admin_news1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_news1.errors.count, "error") %> prohibited this admin_news1 from being saved:</h2>

      <ul>
        <% admin_news1.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
